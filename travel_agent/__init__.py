"""AI Travel Agent — conversational CLI agent that builds live travel plans.

Flights, hotels, activities and weather are fetched through real APIs via
function calling. Nothing is ever invented: if live data is unavailable the
agent labels that section "estimate — verify before booking."
"""

__version__ = "1.0.0"
__all__ = ["__version__"]
